@{
    Layout = "~/Views/Shared/_LayoutAfterLogin.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@HttpUtility.UrlDecode(Request.Cookies["PASS.LoginInfo"]["CourseName"]) 作業清單</title>
</head>

<link href="~/Content/jquery-ui.min.css" rel="stylesheet" />
<style>
    .font-set {
        color: darkgreen;
    }

    table {
        margin-left: auto;
        margin-right: auto;
    }

    th {
        align: center;
    }

    label, input { display:block; }
    input.text { margin-bottom:12px; width:95%; padding: .4em; }
    fieldset { padding:0; border:0; margin-top:25px; }
    h1 { font-size: 1.2em; margin: .6em 0; }
    div#users-contain { width: 350px; margin: 20px 0; }
    div#users-contain table { margin: 1em 0; border-collapse: collapse; width: 100%; }
    div#users-contain table td, div#users-contain table th { border: 1px solid #eee; padding: .6em 10px; text-align: left; }
    .ui-dialog .ui-state-error { padding: .3em; }
    .validateTips { border: 1px solid transparent; padding: 0.3em; }
</style>
<body>
    <script src="~/Scripts/jquery-1.12.4.min.js"></script>
    <script src="~/Scripts/jquery.form.min.js"></script>
    <script src="~/Scripts/jquery-ui-1.12.1.min.js"></script>
    <script src="~/Scripts/jquery-dateFormat.js"></script>
    <script src="~/Scripts/Submission.js"></script>

    <div id="dialog-form" title="作業上傳">
        <p id="assignment" class="validateTips font-set"></p>
        <p id="description" class="validateTips"></p>
        <form enctype="multipart/form-data">
            <fieldset>
                <input name="file" id="file" type="file" />
                <progress></progress>
                <!-- Allow form submission with keyboard without duplicating the dialog button -->
                <input type="submit" tabindex="-1" style="position:absolute; top:-1000px">
            </fieldset>
        </form>
    </div>

    <div>
        <h1 align="center" class="font-set">@HttpUtility.UrlDecode(Request.Cookies["PASS.LoginInfo"]["CourseName"]) 作業清單</h1>
        <div align="center">
            <div id="here_table"></div>
        </div>
    </div>
</body>
</html>